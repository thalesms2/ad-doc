---
sidebar_position: 6
---

# Inserir tabela ATU001 backups recém atualizados
Backups recém atualizados normalmente vêm com a tabela atu001 faltando.
Comando para inserir a tabela ATU001:
```sql
CREATE TABLE public.atu001
(
    appnom character(20) COLLATE pg_catalog."default" NOT NULL,
    appdata timestamp without time zone,
    appbin bytea,
    CONSTRAINT atu001_pkey PRIMARY KEY (appnom)
)
WITH (
    OIDS = FALSE
)
TABLESPACE pg_default;

ALTER TABLE public.atu001
    OWNER to postgres;
```

