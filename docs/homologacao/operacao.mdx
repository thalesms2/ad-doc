# Configuração de natureza de operação de nota

## Devolução

Deve estar como nota referenciada: `Sim, XML`, pois a nota somente `Sim` não referencia no XML e funciona só internamente, 
a opção `Sim, XML` faz a referencia da nota dentro do XML gerado para envio para a SEFAZ